import { supabase } from '@/lib/supabase'; 
import { NextResponse } from 'next/server';
const sh=p=>({id:p.id,billId:p.bill_id,mode:p.mode,amount:+p.amount,date:p.date,partyName:p.party_name,city:p.city,remarks:p.remarks,upiApp:p.upi_app||'',upiRef:p.upi_ref||'',chequeNo:p.cheque_no||'',bank:p.bank||'',receivedDate:p.received_date||'',chequeDate:p.cheque_date||'',clearanceDate:p.clearance_date||'',areaName:p.area_name||'',chequeStatus:p.cheque_status||'',createdAt:p.created_at});
export async function GET(){const{data,error}=await supabase.from('payments').select('*').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data.map(sh));}
export async function POST(req){const b=await req.json();const{data,error}=await supabase.from('payments').insert([{bill_id:b.billId||null,mode:b.mode,amount:b.amount,date:b.date||null,party_name:b.partyName,city:b.city,remarks:b.remarks,upi_app:b.upiApp||'',upi_ref:b.upiRef||'',cheque_no:b.chequeNo||'',bank:b.bank||'',received_date:b.receivedDate||null,cheque_date:b.chequeDate||null,clearance_date:b.clearanceDate||null,area_name:b.areaName||'',cheque_status:b.chequeStatus||''}]).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(sh(data),{status:201});}
export async function PATCH(req){const{id,chequeStatus}=await req.json();const{data,error}=await supabase.from('payments').update({cheque_status:chequeStatus}).eq('id',id).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(sh(data));}
