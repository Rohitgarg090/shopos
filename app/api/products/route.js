import { supabase } from '@/lib/supabase'; import { NextResponse } from 'next/server';
const shape=p=>({id:p.id,name:p.name,sku:p.sku,cat:p.cat,sub:p.sub||'',size:p.size,color:p.color||'',price:+p.price,gst:+p.gst,qty:+p.qty,hsn:p.hsn||'',articleNo:p.article_no||''});
export async function GET(){const{data,error}=await supabase.from('products').select('*').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data.map(shape));}
export async function POST(req){const b=await req.json();const{data,error}=await supabase.from('products').insert([{name:b.name,sku:b.sku,cat:b.cat,sub:b.sub||'',size:b.size,color:b.color||'',price:+b.price,gst:+b.gst,qty:+b.qty,hsn:b.hsn||'',article_no:b.articleNo||''}]).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(shape(data),{status:201});}
export async function PUT(req){const b=await req.json();const{id,...f}=b;const{data,error}=await supabase.from('products').update({name:f.name,sku:f.sku,cat:f.cat,sub:f.sub||'',size:f.size,color:f.color||'',price:+f.price,gst:+f.gst,qty:+f.qty,hsn:f.hsn||'',article_no:f.articleNo||''}).eq('id',id).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(shape(data));}
export async function DELETE(req){const{searchParams}=new URL(req.url);const id=searchParams.get('id');const{error}=await supabase.from('products').delete().eq('id',id);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({success:true});}
