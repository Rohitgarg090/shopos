import { supabase } from '@/lib/supabase';
import { NextResponse } from 'next/server';
const shape=c=>({id:c.id,name:c.name,phone:c.phone,shopname:c.shopname||'',gst:c.gst||'',addr:c.addr||'',email:c.email||'',openingBalance:+c.opening_balance||0,openingBalanceDate:c.opening_balance_date||''});
export async function GET(){const{data,error}=await supabase.from('customers').select('*').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data.map(shape));}
export async function POST(req){const b=await req.json();const{data,error}=await supabase.from('customers').insert([{name:b.name,phone:b.phone,shopname:b.shopname||'',gst:b.gst||'',addr:b.addr||'',email:b.email||'',opening_balance:+b.openingBalance||0,opening_balance_date:b.openingBalanceDate||null}]).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(shape(data),{status:201});}
export async function PATCH(req){const b=await req.json();const{id,openingBalance,openingBalanceDate,...rest}=b;const u={};if(rest.name)u.name=rest.name;if(rest.phone)u.phone=rest.phone;if(rest.shopname!==undefined)u.shopname=rest.shopname;if(rest.gst!==undefined)u.gst=rest.gst;if(rest.addr!==undefined)u.addr=rest.addr;if(rest.email!==undefined)u.email=rest.email;if(openingBalance!==undefined)u.opening_balance=+openingBalance;if(openingBalanceDate!==undefined)u.opening_balance_date=openingBalanceDate||null;const{data,error}=await supabase.from('customers').update(u).eq('id',id).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(shape(data));}
export async function DELETE(req){const{searchParams}=new URL(req.url);const id=searchParams.get('id');const{error}=await supabase.from('customers').delete().eq('id',id);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({success:true});}